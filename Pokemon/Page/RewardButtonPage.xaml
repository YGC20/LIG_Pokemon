<Page x:Class="Pokemon.Page.RewardButtonPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      mc:Ignorable="d"
      d:DesignHeight="250" d:DesignWidth="800"
      Title="RewardButtonPage">

    <Page.Resources>
        <Style x:Key="RewardButtonStyle" TargetType="Button">
            <Setter Property="Margin" Value="2"/>
            <Setter Property="FontSize" Value="18"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Foreground" Value="#5A3D8C"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="ButtonBorder"
                                Background="#E6FFFFFF"
                                BorderBrush="GreenYellow"
                                BorderThickness="5"
                                CornerRadius="16">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="ButtonBorder" Property="Background" Value="White"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="ButtonBorder" Property="Background" Value="#DDDDDD"/>
                                <Setter TargetName="ButtonBorder" Property="BorderBrush" Value="Gray"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Page.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Button x:Name="Reward1Button" Grid.Column="0" Content="EMPTY" Style="{StaticResource RewardButtonStyle}" Click="Reward1ButtonClick"/>
        <Button x:Name="Reward2Button" Grid.Column="1" Content="EMPTY" Style="{StaticResource RewardButtonStyle}" Click="Reward2ButtonClick"/>
        <Button x:Name="Reward3Button" Grid.Column="2" Content="EMPTY" Style="{StaticResource RewardButtonStyle}" Click="Reward3ButtonClick"/>
    </Grid>
</Page>
